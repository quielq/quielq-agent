# Phase 0 test agent: proves the tool-call loop using Ollama cloud's built-in
# web_search API (same OLLAMA_API_KEY, no separate search provider needed).
name: research
display_name: "Research"
# Placeholder - verify against `ollama list` (signed in) or the Ollama cloud
# catalog page before running. Do not trust this name blindly.
model: qwen3:235b-cloud
system_prompt_file: fleet/prompts/research.md
tools:
  allow: [web_search]
memory_dir: fleet/memory/research
